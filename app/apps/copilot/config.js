// app/apps/copilot/config.js
// Single source of truth for this app's display name — used by page.js
// (chat UI text) and layout.js (page title / metadata). Change it here
// once instead of hunting through strings if you rename it again.
export const APP_NAME = 'Purple Giraffe Copilot';
export const APP_TAGLINE = 'AI assistant for AdTech FAQs & debugging';
